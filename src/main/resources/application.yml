spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mestizotour?useSSL=false
    username: root
    password: brabu116
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update

server:
  port: 9090